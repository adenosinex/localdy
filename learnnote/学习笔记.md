# 短视频浏览项目 - 学习笔记

## 项目概述
本项目实现了类似抖音网页版的短视频浏览系统，核心特性是**3视频预加载机制**，实现流畅的视频切换体验。

---

## 📁 文件结构与知识点

### 1. `templates/index.html` - 前端模板
**核心知识点：**

#### Vue.js 模板语法
```html
<!-- 条件渲染 -->
<div v-if="isLoading" class="loading-screen">
<div v-show="!isLoading && state.videos.length">

<!-- 列表渲染 -->
<video v-for="(video, index) in videoQueue" :key="video.id">

<!-- 动态属性绑定 -->
:class="[videoClass, { 'active-video': index === activeVideoIndex }]"
:muted="index !== activeVideoIndex"
:controls="index === activeVideoIndex"
```

#### 3视频预加载系统HTML结构
```html
<div class="video-container">
    <video
        v-for="(video, index) in videoQueue"
        :key="video.id"
        :src="getVideoUrl(video.id)"
        :class="[videoClass, { 'active-video': index === activeVideoIndex, 'hidden-video': index !== activeVideoIndex }]"
        :muted="index !== activeVideoIndex"
        :controls="index === activeVideoIndex"
        preload="auto"
        playsinline
        @ended="nextVideo"
        @loadeddata="onVideoLoaded(index)"
        @canplaythrough="onVideoReady(index)"
    ></video>
</div>
```

#### 关键HTML属性
- `preload="auto"`: 自动预加载视频
- `playsinline`: 移动端内联播放
- `:muted`: 动态静音控制
- `:controls`: 动态显示控制条

#### 加载状态管理
```html
<!-- 加载屏幕 -->
<div v-if="isLoading" class="loading-screen">
    <img src="/static/logo.png" alt="LOGO">
    <div>加载中...</div>
</div>

<!-- 视频界面 -->
<div class="video-box" v-show="!isLoading && state.videos.length">
```

---

### 2. `static/js/main.js` - 核心JavaScript逻辑
**核心知识点：**

#### Vue 3 Composition API
```javascript
const { createApp, reactive, onMounted, computed, ref } = Vue

// 响应式状态管理
const state = reactive({
    videos: [],
    page: 1,
    pageSize: 10,
})

// 响应式引用
const currentIndex = ref(0)
const videoQueue = ref([])
const activeVideoIndex = ref(1) // 中间视频为活跃视频
const isLoading = ref(true)
```

#### 3视频预加载核心算法
```javascript
// 初始化视频队列
function initializeVideoQueue() {
    if (state.videos.length === 0) return
    
    videoQueue.value = []
    activeVideoIndex.value = 1
    videoLoadStates.value = [false, false, false]
    
    // 填充3个视频：前一个、当前、下一个
    for (let i = 0; i < 3; i++) {
        const videoIndex = (currentIndex.value - 1 + i + state.videos.length) % state.videos.length
        if (state.videos[videoIndex]) {
            videoQueue.value.push(state.videos[videoIndex])
        }
    }
}

// 更新视频队列（TikTok风格）
function updateVideoQueue(direction = 'next') {
    if (state.videos.length === 0) return
    
    if (direction === 'next') {
        // 删除第一个视频，添加新视频到末尾
        videoQueue.value.shift()
        const nextVideoIndex = (currentIndex.value + 1) % state.videos.length
        if (state.videos[nextVideoIndex]) {
            videoQueue.value.push(state.videos[nextVideoIndex])
        }
    } else {
        // 删除最后一个视频，添加新视频到开头
        videoQueue.value.pop()
        const prevVideoIndex = (currentIndex.value - 2 + state.videos.length) % state.videos.length
        if (state.videos[prevVideoIndex]) {
            videoQueue.value.unshift(state.videos[prevVideoIndex])
        }
    }
}
```

#### 视频播放控制优化
```javascript
function playActiveVideo() {
    const videos = document.querySelectorAll('video')
    if (videos[activeVideoIndex.value]) {
        const activeVideo = videos[activeVideoIndex.value]
        
        // 只取消静音和播放，不重新开始（除非必要）
        activeVideo.muted = false
        
        // 只在视频未开始或已结束时重置时间
        if (activeVideo.currentTime === 0 || activeVideo.ended) {
            activeVideo.currentTime = 0
        }
        
        activeVideo.play().catch(e => console.log('Play failed:', e))
        
        // 视频类别更新（防止重复计算）
        if (!activeVideo.hasAttribute('data-class-updated')) {
            activeVideo.onloadedmetadata = () => {
                updateVideoClass(activeVideo)
                activeVideo.setAttribute('data-class-updated', 'true')
            }
        }
    }
}
```

#### 视频切换逻辑
```javascript
async function nextVideo() {
    if (state.videos.length === 0) return
    
    // 暂停当前视频
    const currentVideo = document.querySelectorAll('video')[activeVideoIndex.value]
    if (currentVideo) {
        currentVideo.pause()
        currentVideo.muted = true
    }
    
    // 检查是否需要加载更多视频
    if (currentIndex.value >= state.videos.length - 2) {
        state.page++
        await loadVideos(true)
    }
    
    // 更新索引和队列
    currentIndex.value = (currentIndex.value + 1) % state.videos.length
    updateVideoQueue('next')
    
    // 播放中间视频
    setTimeout(() => {
        playActiveVideo()
    }, 200)
}
```

#### 触摸手势处理
```javascript
function handleTouchStart(e) {
    const videoEl = document.elementFromPoint(
        e.touches[0].clientX,
        e.touches[0].clientY
    )
    if (videoEl && videoEl.tagName === 'VIDEO') {
        touchActive = true
        touchStartY = e.touches[0].clientY
    }
}

function handleTouchEnd(e) {
    if (!touchActive) return
    const deltaY = e.changedTouches[0].clientY - touchStartY
    if (Math.abs(deltaY) > 50) {
        if (deltaY < 0) nextVideo() // 向上滑动
        else prevVideo() // 向下滑动
    }
    touchActive = false
}
```

#### 计算属性与响应式
```javascript
const currentVideo = computed(() => {
    if (videoQueue.value.length > 0 && activeVideoIndex.value < videoQueue.value.length) {
        return videoQueue.value[activeVideoIndex.value] || {}
    }
    return state.videos[currentIndex.value] || {}
})
```

---

### 3. `static/css/style.css` - 样式设计
**核心知识点：**

#### 视频容器布局
```css
.video-container {
    position: relative;
    width: 100%;
    height: 100%;
    display: flex;
    justify-content: center;
    align-items: center;
}

video {
    background: #000;
    border-radius: 0;
    object-fit: cover;
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    transition: opacity 0.3s ease; /* 平滑过渡 */
}
```

#### 视频状态控制
```css
.active-video {
    opacity: 1;
    z-index: 2; /* 活跃视频在最上层 */
}

.hidden-video {
    opacity: 0;
    z-index: 1; /* 隐藏视频在下层 */
}
```

#### 响应式视频尺寸
```css
/* 竖屏视频样式 */
.video-portrait {
    height: auto;
    width: 44vh;
}

/* 横屏视频样式 */
.video-landscape {
    height: 60vh;
    max-width: 95%;
}
```

#### 加载屏幕设计
```css
.loading-screen {
    position: fixed;
    top: 0;
    left: 0;
    width: 100vw;
    height: 100vh;
    background: #000;
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    z-index: 1000;
}
```

---

### 4. `api_base.js` - API基础模块
**核心知识点：**

#### 模块化导出
```javascript
export async function fetchLatestVideos(state) {
    // API调用逻辑
}

export function getVideoUrl(videoId) {
    return `/videos/${videoId}`
}
```

---

### 5. `app.py` - Flask后端
**核心知识点：**

#### Flask路由设计
```python
@app.route('/')
def index():
    return render_template('index.html')

@app.route('/videos/<int:video_id>')
def get_video(video_id):
    # 视频文件服务
```

---

## 🎯 核心技术要点总结

### 1. **3视频预加载机制**
- **队列管理**: 始终维持3个视频（前、中、后）
- **动态更新**: 切换时删除最旧视频，添加新视频
- **内存优化**: 只保持3个视频在内存中

### 2. **性能优化策略**
- **预加载**: `preload="auto"` 自动预加载
- **智能播放**: 避免不必要的视频重启
- **选择性暂停**: 只暂停非活跃视频
- **元数据缓存**: 避免重复计算视频尺寸

### 3. **用户体验优化**
- **加载状态**: 优雅的加载屏幕
- **平滑过渡**: CSS过渡动画
- **多种控制**: 触摸、键盘、鼠标滚轮
- **响应式设计**: 适配不同屏幕尺寸

### 4. **Vue.js最佳实践**
- **Composition API**: 现代Vue开发模式
- **响应式状态**: reactive和ref的合理使用
- **计算属性**: 性能优化的数据计算
- **生命周期**: onMounted的正确使用

### 5. **移动端适配**
- **触摸手势**: 上下滑动切换视频
- **内联播放**: playsinline属性
- **视口适配**: viewport meta标签
- **触摸优化**: 防止误触和提升响应性

---

## 🚀 项目亮点

1. **类TikTok体验**: 流畅的视频切换，无加载延迟
2. **内存高效**: 智能的3视频缓存机制
3. **跨平台兼容**: 支持桌面和移动端
4. **性能优化**: 减少视频重启和闪烁
5. **用户友好**: 直观的加载状态和错误处理

这个项目展示了现代Web开发中视频处理、状态管理、性能优化等多个重要概念的实际应用。
